/**
 * The API for the demo project.
 */
@org.osgi.annotation.versioning.Version("1.0.0")
package net.yetamine.lectures.osgi.demo.api;

/* Notes:
 *
 * Using versioning annotations is probably an overkill for a bundle that has
 * just a single package because the package version matches usually the bundle
 * version and the Maven plugin automatically assigns the bundle version to any
 * package that specify no version explicitly.
 */
